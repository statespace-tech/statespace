//! Embedded templates (AGENTS.md, favicon.svg, index.html).
//!
//! Re-exported from the shared `statespace-templates` crate.

pub use statespace_templates::{AGENTS_MD, FAVICON_SVG, render_index_html};

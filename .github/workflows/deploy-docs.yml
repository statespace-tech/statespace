name: Deploy Documentation

on:
  workflow_dispatch:  # Allow manual triggering from GitHub Actions tab
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'zensical.toml'
      - 'binaries/statespace-cli/src/**'

jobs:
  deploy:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Generate CLI docs
        run: cargo run --bin gen-cli-docs

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate docs nav targets
        run: python3 scripts/check_docs_nav.py
      
      - name: Deploy to Fly.io
        run: |
          cd docs
          SKIP_DOCS_NAV_CHECK=1 bash deploy.sh
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
